{
  "name": "verceldemo",
  "version": "1.0.0",
  "description": "",
  "main": "./backend/app.js",
  "scripts": {
    "start": "cd ./backend && node app.js",
	"install": "cd ./frontend && yarn install && yarn build && cd .. && cd ./backend && yarn install",
	"build-all": "cd ./frontend && yarn install && yarn build && cd .. && cd ./backend && yarn install",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^8.22.0"
  }
}
